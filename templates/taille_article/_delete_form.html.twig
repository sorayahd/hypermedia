<form method="post" action="{{ path('app_taille_article_delete', {'articls': taille_article.articls}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taille_article.articls) }}">
    <button class="btn">Delete</button>
</form>
