{% extends 'index.html.twig' %}

{% block title %}Adresse{% endblock %}

{% block body %}
<div class="container">
    <h1>Adresse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adresse.id }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ adresse.adresse }}</td>
            </tr>
            <tr>
                <th>Complement</th>
                <td>{{ adresse.complement }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ adresse.ville }}</td>
            </tr>
            <tr>
                <th>Postale</th>
                <td>{{ adresse.postale }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ adresse.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user') }}">consulter</a>

    <a href="{{ path('app_adresse_edit', {'id': adresse.id}) }}">modifier</a>
</div>
    {{ include('adresse/_delete_form.html.twig') }}
{% endblock %}
