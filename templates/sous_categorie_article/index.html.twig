{% extends 'baseAdmin.html.twig' %}

{% block title %}SousCategorieArticle index{% endblock %}

{% block body %}
    <h1>SousCategorieArticle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>categorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sous_categorie_article in sous_categorie_articles %}
            <tr>
                <td>{{ sous_categorie_article.id }}</td>
                <td>{{ sous_categorie_article.nom }}</td>
                <td>{{ sous_categorie_article.Categorie}}</td>
                <td>
                    <a href="{{ path('app_sous_categorie_article_show', {'id': sous_categorie_article.id}) }}">show</a>
                    <a href="{{ path('app_sous_categorie_article_edit', {'id': sous_categorie_article.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_sous_categorie_article_new') }}">Create new</a>
{% endblock %}
